'''
    递归函数 ： 自己调用自己，
        1.可以用来计算列表的累加和，等等之类的操作

'''

# 1. 求 1 - n 的累加和 ： 这个递归函数，还是可以好好理解的，因为编程的时候很少用到
def my_add(n):
    if n == 0: # 这个位置就是 结束的 点，必须要有的
        return 0
    else:
        return n + my_add(n-1) # 在这里再调用一下自己

res = my_add(100)
print(res)
